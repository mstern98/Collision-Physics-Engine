
#include <stdio.h>
#include "../include/Object/*"
#include "../include/DataTypes/*"

// currently this is test code

int main() 
{
	Vector** vec1 = {vector_init(0, 0, 0), vector_init(1, 1, 1);
	Vector** vec1 = {vector_init(.5, .5, .5), vector_init(-1, -1, -1);

	printf("vec1 x: %f vec1 y: %fvec1 z: %f \n", vec1->x, vec1->y, vec1->z);
	
	
	
	Object* obj1 = obj_init("cheese", 2, vec1);
	Object* obj2 = obj_init("crackers", 2, vec2);
	
	
	return 0;
}
